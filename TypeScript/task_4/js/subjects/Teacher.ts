// Defining the Teacher interface within the Subjects namespace
namespace Subjects {
  // Interface representing teacher details
  export interface Teacher {
    firstName: string; // First name of the teacher
    lastName: string;  // Last name of the teacher
  }
}
